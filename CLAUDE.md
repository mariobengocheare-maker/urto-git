# URTO — Ultimate Realtor Tool

Solo-user local tool for a Miami-Dade real estate agent (Mario Bengochea). Flask + vanilla HTML/CSS/JS — **no build step, no npm install, no React/Tailwind/TypeScript**. Runs as `python app.py`, opened at `http://localhost:5000` on the user's own Windows PC.

## Current status

**Everything below is built, working, and verified — not a plan, not in progress.** Working tree is clean; every commit is pushed to `origin/claude/setup-confirmation-oyq8oa`. There is no known open bug and no half-finished feature. The natural next step in a new session is whatever Mario asks for next — don't assume there's a backlog to pick up.

**⚠️ Branch note:** `claude/hi-dvwvqw` and `claude/setup-confirmation-oyq8oa` are both older branches referenced in earlier docs/commits — they are **stale**. All current work is on **`claude/context-window-dgen3k`**, which is ahead of both. Push there unless Mario explicitly says otherwise. If a fresh session was auto-assigned yet another new branch name, check `git log` on it before assuming it has the latest work — rebase onto/compare against `origin/claude/context-window-dgen3k` if unsure. (This has happened twice now — always verify which branch actually has the newest commits rather than trusting the last name written down here.)

Build order, roughly (see `git log` for exact commits):
1. Owner Lookup CSV automation (Playwright + FOREWARN) — CLI first, then a Flask/drag-drop web UI.
2. Hardened it against real bugs found by the user running it live: MUI selector failures, a wrong-person phone-matching bug, human-like typing/pacing, a Stop button, always-available partial-results download.
3. Rebranded the page as "URTO" (navy/gold design).
4. Added URTO CRM (SQLite-backed clients/notes/recurring-follow-up calendar) and URTO Dialer (click-to-call power dialer) as new tabs.
5. Replaced the Home tab with a from-scratch WebGL space scene (Three.js, vendored), orbs as the only nav on Home.
6. Added a decorative skywriting rocket on Home, refined twice for legibility.
7. Wrote first CLAUDE.md session handoff.
8. Upgraded the rocket from a flat canvas sprite to a real lit 3D mesh (nose/body/fins/nozzle/window), layered flame + spark particles, shaded volumetric smoke.
9. Added automatic CRM backup: instant OneDrive mirror (`urto_crm_latest.db`) refreshed on every client/note/event change (including deletions), plus periodic timestamped snapshots. Auto-detects OneDrive via env var, falls back to a local `backups/` folder.
10. Rewrote CSV parsing (`input_parser.py`) to match columns by meaning/alias instead of exact hardcoded names — fixes real-world exports (e.g. Miami-Dade GIS using "Postal Code"/"State Abbreviation") that were being rejected outright.
11. Added manual calendar events (click a day → add/edit modal, optionally linked to a CRM client as a follow-up), a "Today's Follow-ups" Dialer source, and a live "In your CRM" info panel on the Dialer (phone/notes/follow-up status) whenever the current entry matches a client — including by phone-number cross-reference for pasted/uploaded lists.
12. Added "+ Add to CRM" on the Dialer for non-CRM leads — prefills the shared client form from the dialer entry.
13. Full cosmetic pass: brought the WebGL home screen's glass/starfield/gold-glow look to every other page, modal, and control. Purely visual, no feature changes.
14. Added free, in-browser voice-dictated call notes on the Dialer (Web Speech API, no API key, no cost) — a "🎙 Dictate a call note" button transcribes a spoken recap and saves it straight to the current client's CRM notes.
15. Cosmetic pass #2: reworked the skywriting rocket (removed vertical bob, sleeker model) and rebuilt "Made by Mario Bengochea" as a GPU smoke point-cloud that's written on column-by-column instead of a flat label fade-in; tightened point density/size/brightness after Mario flagged it as blurry. Separately, extended dictated call notes to work for **any** dialer entry, not just existing CRM clients — saving a note for a non-CRM lead now auto-creates a lightweight contact from the dial entry so the note has somewhere to live.
16. Added a Windows desktop-icon launcher (`launch_desktop.pyw` + one-time `Create URTO Desktop Icon.vbs`) — Mario can now double-click a "URTO" desktop icon instead of opening a terminal; it starts the Flask server silently (no console window, via pythonw.exe) and opens the app in his normal browser. Re-running the `.vbs` is only needed if he moves the project to a new folder.
17. Gave the desktop icon a custom look — `static/urto_icon.ico` (multi-resolution, generated with Pillow: navy glass badge, gold-gradient house/rocket-nose glyph matching the app favicon's silhouette, soft glow/shadow, thin gold border ring) instead of the default Python icon. The `.vbs` points `IconLocation` at it. The Pillow generator script itself isn't committed (one-off asset creation, not a runtime dependency — Pillow is intentionally **not** in `requirements.txt`); regenerate by redrawing at ~1024px and re-exporting sizes [16,24,32,48,64,128,256] into one `.ico` if the design ever needs to change.
18. Added LLC/business-entity owner resolution (`llc_lookup.py`) — a row owned by an LLC/Inc/Corp/etc. is no longer skipped outright. It's resolved to a real person (registered agent, or an officer/manager if the agent is a company) via a live Florida Sunbiz search, in the same shared visible Chromium window as FOREWARN, then fed into the normal name+zip FOREWARN search. Chose this over Florida's official bulk-data SFTP feed (also researched — free, no login, but a large government data dump requiring local ETL + periodic refresh, deemed too heavy for what Mario needed) after explicit discussion with Mario. **Confirmed working against the live site** — Mario ran a real county CSV and got back multiple correct `Resolved via Sunbiz (registered agent): '...' -> Name` rows plus an accurate "no matching entity found" for one that genuinely wasn't there. The selectors built blind (see "Hard-won gotchas") held up on the first real try.
19. Added trust-name person extraction (`input_parser.py`, `_try_extract_trust_person`) — a revocable/living/family trust is very often named after its own settlor/trustee (`MAHONEY MARY ANN REVOCABLE TRUST`), so trust-suffix words and a trailing creation date are stripped and the same known "LAST FIRST [MIDDLE]" county convention is applied to what's left. Only resolves when a real first name remains — `AVILES FAMILY TRUST`/`FINTLAND FAMILY TRUST 03/11/2008` have just a surname (+ maybe a date) and stay correctly skipped rather than guessing. No Sunbiz involved — trusts aren't registered there, this is pure string parsing on the input row.
20. Added a persistent weekly lookup counter, always visible on the Owner Lookup tab (e.g. "12 lookups this week (Jul 27 – Aug 2)") — Mario wanted to track FOREWARN usage against a weekly quota. Counts once per actual FOREWARN search attempt (`lookup_engine._run_forewarn_search`), not per input row — a row skipped at input-parsing time, or an LLC that fails Sunbiz resolution, never reaches FOREWARN and doesn't count. Stored in a new `lookup_stats` table in the existing `urto_crm.db` (keyed by that week's Monday date), so it survives restarts and rides along with the existing backup system for free. The week "resets" itself automatically — a new week just doesn't have a row yet, no explicit reset job needed.
21. Fixed the Dialer's "Paste / Upload List" mangling a pasted results-CSV row into one jumbled name blob — it only understood simple "Name, Phone" lines (find one phone number via regex, treat the rest of the line as the name). `parseDialerText` in `templates/index.html` now recognizes real CSV structure first: a proper quoted-field-aware `splitCsvLine`, a recognized header row (name/phone/address by alias, same by-meaning philosophy as `input_parser.py`), or — Mario's actual reported case — a bare headerless row matching URTO's own 9-column results-CSV shape (`RESULTS_CSV_FIELDS`, kept in sync with `OUTPUT_FIELDS`). Only falls back to the old single-phone-regex heuristic per-line when a line matches neither recognized shape, so plain "Jose Bengochea, 305-555-1212" pasting still works unchanged.
22. Added **Transaction Manager** — a whole new section (5th Home orb, "Transactions" tab) for tracking required documents through closing on a deal. Four fixed transaction types (Listing, Rental Listing, Buyer Representation, Rental), each with its own **shared, editable document checklist** (`document_types` table) — a blank template file can be uploaded once per document and is reused across every transaction of that type. Creating a transaction **snapshots** the type's current checklist into its own independent rows (`transaction_documents`) — editing/removing a document from the type's checklist afterward only affects transactions created from then on; existing ones keep whatever they had (and any signed upload) untouched. Each row tracks its own signed-copy upload separately. Transaction status (Active/Under Contract/Closed) shown as a green/yellow/gold pill. Uploaded files (`documents/templates/`, `documents/signed/`, both gitignored) are mirrored into the existing OneDrive/local backup folder — real-file mirroring only happens on the periodic/snapshot backup path (not the high-frequency instant-mirror-per-edit path), with proper deletion propagation so a removed document actually disappears from the backup too. Built after explicit discussion with Mario on three design forks (shared-per-type vs. per-transaction checklist; shared vs. per-transaction template file; whether documents ride along with the CRM backup system) — he chose the simpler/shared option each time.
23. Fixed three Transaction Manager bugs Mario hit while actually using it: (1) "Manage Document Checklists" → Add Document silently did nothing on an empty name field — now shows a visible inline error and also submits on Enter; (2) document checklist rows can now be drag-reordered (`reorder_document_types` in `crm.py`, persisted via `/api/txn/document_types/reorder`); (3) the Closed status pill/button was rendering visually indistinguishable from the yellow Under Contract one — replaced the unstyled native status `<select>` in the detail view with a custom 3-button segmented control, and gave Closed its own hardcoded richer metallic-gold gradient (not the shared `--gold`/`--gold-bright` vars, which read too close to the warn-yellow at a glance) instead of reusing branding colors used correctly elsewhere.
24. Added a **"Closings" folder** for transactions (a single `archived` boolean on `transactions`) — **superseded one round later by #26's three-way `folder` column**, see below.
25. Toned down the Home scene's overall glow (Mario: "decrease the shininess of everything, its a bit blinding") — lower bloom-pass strength (0.5→0.32) and threshold tweak, halved orb glow-sprite opacity/scale and emissive intensity, dimmer point-lights, softer nebula/ring opacities — while deliberately leaving the URTO logo's own material (metalness/clearcoat/emissive) and the key/rim directional lights untouched, since Mario said he still likes that specific shine. Also **removed the skywriting rocket entirely** (deleted `buildFlyby`/`buildRocketGroup`/`buildFlameGroup`/`buildSkywriting` and the whole puff/spark particle system from `static/urto-space.js`) and replaced it with a small always-visible static `#spaceCredit` line ("Made by Mario Bengochea") under the tagline — no animation, no timing logic to babysit.
26. Reworked transaction filing into **two separate folders**: `transactions.folder` (TEXT: `'active'`/`'closings'`/`'archive'`, migration-safe add + one-time backfill from the old `archived` boolean into `'closings'`) replaces the single archived flag from #24. "💰 Closings" and "📁 Archive" are now distinct subtabs on the Transactions list; a transaction's detail view shows "💰 Move to Closings" + "📁 Move to Archive" while in Active, or a single "↩ Restore to Active" once filed in either folder. Also added a delete button (🗑) directly on each transaction list card (not just inside the detail view) so Mario doesn't have to open a transaction first — both delete paths confirm with "Are you sure you want to delete this transaction? This cannot be undone." Also nudged more vertical space between the Home scene's tagline and the "Made by Mario Bengochea" credit line (then nudged it back closer one round later, per Mario: too far the first time).
27. Added **Contact Lists** — a named group of CRM clients (e.g. every unit in one building) with **one shared recurring call schedule for the whole group**, distinct from each client's own individual follow-up schedule. New `contact_lists` + `contact_list_members` (many-to-many) tables in `crm.py`; a list's `next_call_date`/`interval_days` advance together via `complete_list_round()` (same "advance from whatever's currently due" pattern as `complete_followup()`), not per-member. New "Contact Lists" subtab in URTO CRM (create/edit/delete a list, "Manage Members" modal with a searchable checkbox picker over all CRM clients); the client Add/Edit form also got its own Contact Lists checkbox group so membership can be set from either side (`create_client`/`update_client` both take an optional `list_ids`). Dialer got a new "From Contact List" source — pick a saved list, load every member with a phone number, and a "✅ Mark this round as called" button to advance the shared schedule once you're done dialing through it. Built after explicit discussion with Mario on the two open design questions (CRM clients vs. a lighter standalone list; one shared schedule vs. per-contact schedules) — he chose the richer/shared option both times. Seeded Mario's real first list, "9350 Bay Harbor Dr" (16 residents), from a vCard export he provided, in this sandbox's own `urto_crm.db` — never shipped anywhere since that DB file is gitignored.
28. Mario: "I dont want any update i give you to be dependent on ME going into console and pasting any commands" — a standing rule for all future work, not just this feature. In direct response, replaced the just-added CLI `import_contact_list.py` with a proper in-app drag-and-drop: a dropzone on the Contact Lists subtab (`vcardDropzone`/`#clImportOverlay` in `templates/index.html`) opens a small form (list name, optional shared address, call schedule) and posts the file to `/api/crm/contact_lists/import_vcard` (multipart). Also made the parser format-agnostic per Mario's follow-up ("make the drag and drop... any type of file format"): `crm.parse_contact_import()` handles a vCard (`BEGIN:VCARD`), a CSV with a recognized header row (name/phone/address by alias, same by-meaning philosophy as `input_parser.py`), a headerless "Name, Phone[, Address]" CSV shape, or plain pasted lines — falling back **per line** rather than all-or-nothing, so one differently-shaped line in a mixed file doesn't drop the rest. `import_contact_list.py` (the CLI script from #27) is kept as a fallback/scriptable option but is no longer the primary path — the in-app upload is.
29. Fixed two things Mario hit after actually using Contact Lists on his real CRM data: (1) a client whose only call schedule comes from a Contact List (not their own individual follow-up) was showing "No automatic follow-up" — `crm._attach_list_followups()` now joins each client to any Contact List they're on with an active schedule and attaches it as `list_followups`, so the client list/detail views and the Dialer's "In your CRM" panel show "next `<list name>` Follow Up on `<date>`" instead; (2) a Contact List's shared call schedule wasn't appearing on the CRM Calendar at all — `get_calendar_events()` now walks a list's `next_call_date`/`interval_days` the same backward/forward way it already does for individual clients, tagged `kind: "list"`, rendered as a "💰 `<list name>`" chip that jumps to the Contact Lists subtab when clicked.
30. Fixed a real wrong-property bug Mario hit on an LLC-owned parcel: a phone came back correctly matched to the right person (the LLC's Sunbiz-resolved registered agent), but the ADDRESS shown was a different property that person owns, not the parcel Mario was actually asking about — and he confirmed his input CSV never had that address. Root cause: `input_parser.py`'s `HEADER_ALIASES["address"]` (and city/state/zip) checked a generic `"address"`/`"city"`/`"state"` alias BEFORE the more specific `"situs address"`/`"property address"` ones. A county tax-roll export for an entity-owned parcel commonly has BOTH a generic "Address" column (often actually the owner's/registered agent's own MAILING address — where their tax bill goes) and a separate situs/property-address column for the parcel itself; the old alias order silently preferred the generic (often-mailing) column whenever both were present. Fixed by reordering situs/site/property aliases first for address/city/state/zip, and by making `resolve_headers()` hard-exclude any column whose name contains "mailing" outright — it can never stand in for the property's own address/city/state/zip, full stop. Verified with a synthetic CSV shaped exactly like the real failure (generic "Address" = mailing, separate "Situs Address" = the true parcel) plus a same-scenario test combined with LLC entity resolution.
31. Added a visible app version + last-updated timestamp (Mario: "Lost track by now") — an `APP_VERSION` constant at the top of `app.py`, passed into `render_template()` and shown in two places: the site-wide footer (every tab except Home) and a small `#spaceVersion` corner tag on the Home screen (since the footer is hidden there in space-mode). **Bump `APP_VERSION` by hand on every future shipped change** — current: v1.5.2. (v1.4.1 itself was a deliberate no-op bump — Mario wanted to test the new URTO Updater end-to-end on a real update before trusting it with anything substantive. v1.4.2 fixed a real bug in that same updater — see build order #37. **The DATE alongside it stopped being hand-typed as of #43 below** — it kept drifting from reality since I was always guessing Mario's actual local time; see #43 for the real fix.)
32. Fixed a much bigger, systemic version of the same address-matching failure: a results CSV Mario shared showed nearly every row NOT_FOUND — including ones where Sunbiz had correctly resolved an LLC to a real person — with addresses displaying like `"1130.0 NE 7TH AVE 9"` and `"7665.0 SW 88TH CT"`. That trailing `.0` is a classic Excel artifact (a CSV column that's mostly numeric gets saved back out with `.0` appended to whole numbers) — FOREWARN's real address text never has it, so `text_contains_address()` silently failed to match on **every single row carrying the artifact**, regardless of whether the true match actually existed. Added `input_parser._strip_float_artifacts()` (regex-strips a bare `\d+\.0` back to `\d+`) applied to the combined address field, the separate house-number/unit-number columns, and zip — this is a data-quality normalization step, not a column-selection one, so it runs independently of (and after) the mailing-vs-situs alias fix from #30. Verified against the exact addresses from Mario's screenshot.
33. Mario shared a real results CSV (60 rows: 34 FOUND / 14 SKIPPED / 12 NOT_FOUND) asking to analyze the misses. Two were genuine parsing bugs, now fixed: (1) `llc_lookup._split_person_name()` only handled Sunbiz's `"LAST, FIRST"` comma convention — a registered agent with a generational suffix is often listed as a THIRD comma segment, `"LAST, SUFFIX, FIRST"` (e.g. `"HACKETT, II, JOHN"`), which the old first-comma-only split mangled into a garbage name (`"Ii, Hackett"`, `owner` field literally containing a stray comma). Added a specific 3-part-with-known-suffix case (`_NAME_SUFFIXES`) rather than guessing generically. (2) `"PLLC"`/`"PA"`/`"PLC"`/`"PC"`/`"CHARTERED"` (professional-entity suffixes law firms commonly use as their own registered name, e.g. a firm serving as someone's registered agent) weren't in `ENTITY_TOKENS`, so `_looks_like_company()` didn't recognize `"NEIMAN & INTERIAN, PLLC"` as a company and tried to parse it as a person's name (`"Pllc Neiman & Interian"`). Added to both `ENTITY_TOKENS` and `REGISTERED_ENTITY_TOKENS`. Also added `"AGR"`/`"AGMT"`/`"REV"`/`"LIV"`/`"TR"` to `TRUST_STOPWORDS` — `"ZANN ELIZABETH A REV TRUST AGR 4/12/2018"` was skipping outright because trailing-word stripping stopped at the unrecognized `"AGR"`, leaving `"TRUST"` stuck in the middle of the remaining tokens and tripping the entity-token rejection check. The remaining 9 NOT_FOUNDs in that CSV were genuine FOREWARN misses (no candidate's address history matched, or zero FOREWARN results for that name/zip) — not bugs, and not "fixed" by guessing; one (`"TIMOTHY D ONNEN REVOCABLE TRUST"` → `"D Timothy"`) was flagged to Mario as ambiguous-name-order rather than silently changed, since the trust name might be self-authored in plain first-middle-last order instead of the county's usual "LAST FIRST MIDDLE" convention, and guessing wrong here is exactly the wrong-person bug class this file exists to avoid.
34. Mario tried a different data source for the same property (IMAPP's "Detailed Summary" export instead of a raw county tax-roll) and asked if it would help. Two real findings: (1) the file didn't even load — its columns ("Property House Number", "Postal (Zip) Code", "Unit/Apartment/Suite #", etc.) didn't match any known `HEADER_ALIASES`, so `resolve_headers()` found no address columns at all. Added the missing `"property "`-prefixed aliases, and made `_normalize_header()` strip `()#/-` punctuation so decorated headers like `"Postal (Zip) Code"` normalize down to something an alias can match. (2) A much bigger issue: this file's `"Owner Name 1"` column turned out to be in **plain "FIRST [MIDDLE] LAST" order** — the OPPOSITE of the Miami-Dade tax-roll's "LAST FIRST" convention this whole parser was built around — confirmed by cross-referencing several owner names against that same file's recorded-mortgage-borrower field (reliably "LAST FIRST MIDDLE"): `"BRUCE WEMPLE"` / mortgage `"WEMPLE BRUCE M"`, `"JOSE BENGOCHEA"` / mortgage `"BENGOCHEA JOSE"`, `"ROMAN A MARTINEZ"` / mortgage `"MARTINEZ ROMAN A"`. Applying the old assumption would have silently reversed every plain owner name in the file. Added `_detect_name_order()` — checked ONCE per file from a signature of columns that only this kind of aggregator export carries (`"owner address house number"`, `"estimated current value"`, `"total living heated area"`), never guessed per-row or per-name — and threaded a `name_order` param through `parse_owner_name()`/`convert_row()`/`load_rows()`. Also found and handled a real ZIP+4-with-no-dash shape this file uses (`"330364128"` = zip `33036` + plus-4 `4128` concatenated) via `_clean_zip()`. **Trust-owned rows are deliberately EXEMPTED from the detected file-level name_order** and always parsed as `"last_first"` regardless — cross-referencing showed a trust name in this same first-last file (`"SANCHEZ ELLIS A TRUST"` / mortgage `"LIVI ELLIS A SANCHEZ REVOCABLE"`, i.e. Last=Sanchez First=Ellis) still follows the old county convention, apparently carried over verbatim from the raw recorder record rather than run through whatever name-cleanup normalized the plain individual owner rows. Also added `"TR"` to `TRUST_TOKENS` (previously only `"TRUST"/"TRS"/"JTRS"` were recognized as trust indicators) so a trust name using the bare abbreviation (`"OCHIPA RONALD S REV LIV TR AGR 8/1/2001"`) gets routed through trust extraction instead of silently falling through to the generic name path, which — unlike the old `"last_first"`-only world where trailing junk was harmlessly ignored — would otherwise corrupt the result under `"first_last"` (reads the LAST token as the surname, which lands on a stray date/abbreviation instead). One trust row (`"LOURDES BANA REVOCABLE TRUST"`, 2 tokens, no mortgage data to cross-check) has no independent way to verify which name order applies — resolved via the last-first pattern established from the file's other trust rows, but flagged to Mario as the one row in this batch worth a manual double-check.
35. Mario asked whether yet another format (a "Standard Property Record Structure" export — 459 rows, White Marlin Beach subdivision) would only return Islamorada results. It has cleanly-labeled `"Property Address"`/`"Property City"`/`"Property State"`/`"Property Zip Code"` columns (separate from `"Owner Address"`/`"Owner City"`/etc., the out-of-state mailing columns) — but the file still failed to load, since `"Property Zip Code"` didn't match any known zip alias (`HEADER_ALIASES["zip"]` had `"property zip"` and `"zip code"` separately, but not the combined phrase). Added `"property zip code"` as its own alias. Verified end-to-end afterward: all 459 rows parse, every resolved address's city is `ISLAMORADA`/`LOWER MATECUMBE KEY` (never an owner's real mailing city, which ranges across the whole US in this file), `_detect_name_order()` correctly identifies this as the standard `"last_first"` convention (not the IMAPP shape), and spot-checking against the mortgage-borrower field confirmed it (`"GREENWELL JOSEPH R"` / mortgage `"GREENWELL JOSEPH"`, `"WOLFF MARK J"` / mortgage `"WOLFF MARK"`).
36. Mario: manually extracting the ZIP, Ctrl+A/Ctrl+V-ing files over the project folder, and killing the old `python app.py` in Task Manager every single update was "kinda annoying" — asked for a real one-click updater. Added `urto_updater.pyw` + `Create URTO Updater Desktop Icon.vbs`: double-click "URTO Updater" and it downloads the latest branch ZIP, stops any running `app.py` itself (a PowerShell one-liner matched on command line), backs up the current code into `_update_backups/` (code only, not data; capped at 5), installs the new files, and deletes the temp zip/extraction automatically — nothing left to Ctrl+A/Ctrl+V or clean up by hand. Never touches `urto_crm.db`/`backups/`/`outputs/`/`documents/`. Tested the core download → verify → install logic end-to-end against the real GitHub zip in this sandbox (Tkinter itself isn't testable headless here, but the update logic underneath it is the same code either way) — confirmed real code files get replaced while a simulated `urto_crm.db`/`backups/`/`documents/` survive completely untouched. **Bootstrap note:** since the updater doesn't exist on Mario's PC until he installs it once, that first install still has to happen via the old manual ZIP method — after that, every future update goes through the icon.
37. Mario tested the updater live (v1.4.1, deliberately a no-op) and it worked — then asked for the same thing on his other local app, Wardrobe. Before building that, caught and fixed a real latent bug in `urto_updater.pyw` itself: `kill_running_app()` matched any running process by the bare substring `*app.py*` in its command line — but Wardrobe's own Flask entry point is ALSO named `app.py`, so if Mario ever ran both apps at once, updating URTO could have killed Wardrobe's server (or vice versa, once Wardrobe got its own updater) instead of — or in addition to — the intended one. Fixed by matching on the full absolute path to *this folder's* `app.py` (`INSTALL_DIR / "app.py"`) instead of just the filename.
38. Mario reported his weekly lookup counter reset unexpectedly and suspected it was update-related. Root cause: `urto_crm.db` lived **inside** the project folder (`crm.py`'s old `DB_PATH = Path(__file__).parent / "urto_crm.db"`) — the same folder every update installs into/over. Wardrobe already hit and fixed this exact class of bug for its own DB (see its CLAUDE.md) by moving storage outside the project folder entirely; URTO's DB never got the same treatment. Fixed the same way: `crm.py` now resolves `DATA_DIR` to `%LOCALAPPDATA%\URTO` on Windows (falling back to living alongside `crm.py`, e.g. in this sandbox, when there's no `LOCALAPPDATA`), with a one-time `_migrate_legacy_data_dir()` that copies an existing in-project-folder `urto_crm.db`/`documents/` over to the new location the first time the new code runs — so Mario's existing clients/notes/transactions/lookup count all carry forward automatically, no action needed on his end. This protects the WHOLE CRM, not just the counter, from ever resetting like this again, regardless of whether a future update lands in-place or Mario ends up running URTO from a freshly-extracted folder. Verified via a simulated legacy-folder-to-`LOCALAPPDATA` migration test.
39. Mario flagged (via a real FOREWARN results row) that a professional-association registered agent — `"EUGENIO DUARTE, P.A."` — resolved to the garbage name `"P.A. Eugenio Duarte"` instead of being recognized as a company. Root cause: `llc_lookup._looks_like_company()` tokenized by turning `.`/`,`/`()`/`*` all into spaces, so `"P.A."` split into two single-letter tokens (`"P"`, `"A"`) that never matched the already-added `"PA"` entry in `ENTITY_TOKENS` — any dotted entity abbreviation (`"P.A."`, `"L.L.C."`, etc.) was silently invisible to this check. Fixed by stripping periods with no replacement (collapsing `"P.A."` → `"PA"`) before the space-splitting pass. Separately, per Mario's explicit direction ("look up their name in forewarn without PA, but keep BOTH options... higher success rate"): Florida requires a P.A./PLLC/PC-style professional entity to be named after the actual licensed professional running it, so `llc_lookup._try_extract_pa_person()` now strips a trailing `PA_STYLE_SUFFIXES` token and reads the rest as that person's own plain "First Last" name — this becomes the FIRST candidate FOREWARN is searched under (skips the slower Sunbiz officer/manager detour entirely when it's likely already correct), with the existing officer/manager list kept as fallback candidates rather than being replaced. `resolve_entity_owner()` now returns an ordered `candidates` list instead of a single guess, and `lookup_engine.process_row()` retries the next candidate (a real second FOREWARN search, still counted against the weekly quota — see #9) only if the first comes back `NOT_FOUND`. Guards against a multi-partner firm name (`"NEIMAN & INTERIAN, PLLC"`) by refusing to extract when `"&"` appears in the remaining tokens — that names more than one person, not one, so it's left to the officer/manager fallback instead of guessing which partner.

40. Mario asked for backups to also go to Google Drive (not instead of OneDrive — both at once), auto-delete from both clouds when he deletes something in URTO, organized folders (CRM contacts, transaction checklist documents, individual per-transaction document folders) instead of one flat dump, and a single file he can paste into a fresh Claude conversation that describes ALL of URTO's data, regenerated on every change. Explicitly asked first via `AskUserQuestion` how Google Drive should connect, since the real Google Drive API needs Mario to create his own Google Cloud OAuth credentials (a new account/setup step, flaggable per this file's own standing rule) — he picked **Google Drive for Desktop's Mirror-mode local folder** instead, the same zero-setup trick OneDrive's backup already uses. Rebuilt the whole backup system around a destination LIST (`resolve_backup_dirs()`) rather than one-or-the-other, added the CRM Contacts `.txt` export and the full-JSON export (both instant, both cheap), and reorganized the existing flat `documents/` mirror into the two human-named folder structures Mario asked for. See "Backup system" above for the full breakdown. Tested end-to-end with simulated OneDrive/Google Drive folders: contacts export, JSON export, checklist folder, per-transaction folders, and deletion propagation (client deleted, signed doc removed, whole transaction deleted) all verified working in both destinations simultaneously.
41. Mario: "with the backups, have a folder with the CODE DOWNLOAD LINK" — added `_write_code_download_link()` (`crm.py`), writing a plain `CODE_DOWNLOAD_LINK.txt` into every backup destination alongside the data, pointing at the GitHub ZIP/repo URL (kept in sync with `urto_updater.pyw`'s `REPO_ZIP_URL`) plus short numbered recovery steps. Cheap static content, so it's regenerated on every instant mirror like the other exports — makes losing the PC entirely a genuine two-step recovery (GitHub for code, any one backup folder for data + this pointer), not "go find the GitHub link from memory."
42. Mario: "i keep having to close python in my task manager fix that pls" — added the same fix already shipped on Wardrobe (see its CLAUDE.md): the frontend pings `/api/heartbeat` every 5s while a tab is open and fires `/api/shutdown` via `navigator.sendBeacon` on `pagehide`; a `_watchdog()` background thread also force-closes the server (`os._exit(0)`) if no heartbeat arrives for `HEARTBEAT_TIMEOUT` (20s) as a fallback for a crash/force-close/sleep that skips the clean signal. **Difference from Wardrobe**: URTO can have a real headed Chromium window mid-FOREWARN-scrape in a background thread (`run_job`), so both `/api/shutdown` and the watchdog first check `_job_in_progress()` (any `JOBS` entry in `starting`/`awaiting_login`/`running`) and refuse to exit while true — closing the tab mid-lookup must never abandon a real scrape with no partial-results file ever written. Verified via an in-process test: `_job_in_progress()` correctly blocks `os._exit` while a fake running job is registered.
43. Mario reported the footer's "updated" timestamp was wrong again ("It says this last update came at 1:30. Its 12:20 right now") — root cause: `APP_VERSION_DATE` was a string I hand-typed into source at ship time, guessing Mario's actual local time in Eastern, which reliably drifted. Fixed by getting rid of the hand-typed date entirely: `urto_updater.pyw`'s `install_update()` now writes `last_updated.txt` with **this machine's own clock** (`datetime.now().astimezone()`) the instant it finishes installing — real wall-clock time and real local timezone, read straight off whatever PC actually ran the update, never guessed by me. `app.py`'s `_get_last_updated_display()` reads that file and formats it; if the marker doesn't exist yet (a fresh git clone, or an update installed via the old manual-ZIP method before this fix existed), it falls back to `app.py`'s own file mtime, which is still generally close to the real install time. `APP_VERSION` itself is still a hand-bumped semantic version string (that part isn't time-sensitive) — only the DATE stopped being manually typed.

**Explicitly declined / out of scope:** AI-generated call summaries via the Anthropic API were prototyped and then deliberately backed out at Mario's request — he didn't want a per-call API cost or a separate Anthropic billing account. Don't reintroduce that path unless he asks again; the dictation feature (#14) is the free alternative that shipped instead. Similarly, a real Google Drive API integration (OAuth, direct upload without a synced local folder) was discussed and declined in favor of the zero-setup Google Drive for Desktop approach (see #40) — don't build the OAuth path unless Mario explicitly asks and confirms he'll do the one-time Google Cloud credential setup himself.

## User context (read this first)

- **Mario is not a developer.** He needs explicit, numbered, Windows-specific steps for everything. Never assume familiarity with terminals/git beyond what's been walked through already.
- **Standing rule (explicitly stated): no feature should ever require him to open a console/terminal and paste commands.** One-off tasks that used to be "run this script" (e.g. importing a contacts export) must be built as an in-app UI flow instead (upload/drag-and-drop, a button, a form) — see build order #28. If a feature seems to need a script, that's a sign it needs a UI instead, not a sign to hand him a command.
- **Update workflow: the "URTO Updater" desktop icon, not a manual ZIP.** Mario double-clicks "URTO Updater" (see build order #36) and it downloads, installs, and cleans up after itself — no extracting/copying/replacing by hand, no hunting the old process down in Task Manager first. Still give him the ZIP download link (`https://codeload.github.com/mariobengocheare-maker/urto-git/zip/refs/heads/<branch>`) after every push, purely as a manual fallback if the updater itself ever needs fixing (chicken-and-egg — if `urto_updater.pyw` is broken, he can't use it to fix itself).
- **To run it day-to-day, Mario double-clicks the "URTO" Desktop icon** (created once via `Create URTO Desktop Icon.vbs` — see build order #16). No console window, opens straight into his browser. Falls back to the manual command `python app.py` (or `python3 app.py` / `py app.py`) from inside the project folder, then open `http://localhost:5000`, if he ever needs to run it that way.
- **He iterates by screenshot.** Treat screenshots as ground truth over assumptions.
- **Always test before claiming done.** This repo has caught real bugs via headless-Chromium screenshot testing (Playwright, `/opt/pw-browsers/chromium` + `--use-gl=angle --use-angle=swiftshader` for WebGL) before shipping. Don't just eyeball code — render it. For anything gated behind a rare/random event (like the skywriting rocket), force-trigger it via a debug hook during testing rather than waiting — remember to remove the hook before committing.
- **He's cost-conscious about anything beyond the flat ZIP/local-app model.** He asked about cloud-hosting the CRM (discussed, not yet built) and about AI call summaries (built, then explicitly declined over per-use API cost — see status log #14). Always flag clearly, before writing code, when a feature would introduce a recurring cost, a new account, or a dependency beyond his own PC — let him opt in rather than assuming.
- **He enjoys creative/fun asks** (a cat animation during long lookups, a skywriting rocket, a full WebGL home scene) — don't undersell scope on these, but keep them decorative/non-blocking to core functionality.
- Branch: `claude/context-window-dgen3k` on `mariobengocheare-maker/urto-git`. Push directly there unless told otherwise (see branch note above).

## Architecture

- `app.py` — Flask app, all routes (lookup jobs, CRM API, calendar/events API, backup API).
- `launch_desktop.pyw` — Windows double-click entry point (see build order #16). Checks if the server's already up on `127.0.0.1:5000`; if not, spawns `app.py` detached/hidden via `sys.executable` (which is `pythonw.exe` when this `.pyw` itself was launched that way — no console window at any point), waits for it to come up, then opens the browser. Re-launching while already running just opens another browser tab instead of a second server.
- `Create URTO Desktop Icon.vbs` — one-time setup Mario double-clicks to create the "URTO" Desktop shortcut pointing at `launch_desktop.pyw`, with `static/urto_icon.ico` as its icon. Resolves its own folder via `WScript.ScriptFullName` (not the shell's current directory, which isn't reliable for a double-clicked script), so it works regardless of where the project folder lives.
- `static/urto_icon.ico` — the desktop shortcut's custom icon (see build order #17).
- `urto_updater.pyw` — one-click self-updater (see build order #36). Downloads the latest branch ZIP from GitHub, stops any running `app.py` (via a PowerShell `Get-CimInstance`/`Stop-Process` one-liner matched on command line, not by name — never kills an unrelated Python process), backs up the current code (not data) into `_update_backups/` (capped at 5, oldest pruned, same pattern as the DB snapshot cap), installs the new files over this folder, then deletes the temp zip/extraction itself (`tempfile.TemporaryDirectory`) — nothing left for Mario to clean up. Never touches `urto_crm.db`/`backups/`/`outputs/`/`documents/` (a `NEVER_TOUCH` set, on top of those files not shipping in the zip anyway). A small Tkinter status window shows progress and offers a "Launch URTO Now" button when done.
- `Create URTO Updater Desktop Icon.vbs` — one-time setup Mario double-clicks to create the "URTO Updater" Desktop shortcut, mirroring `Create URTO Desktop Icon.vbs`.
- `lookup_engine.py` — Playwright automation of **FOREWARN** (app.forewarn.com). User logs in manually in a headed Chromium window the script opens; automation takes over from there.
- `input_parser.py` — normalizes CSV input into the canonical row format by matching columns **by meaning** (`resolve_headers`/`HEADER_ALIASES`, not exact names) — handles a combined owner-name field (county "LAST FIRST" convention) or separate first/last columns, and either a combined address or house-number/street-name components. Registered-business-entity owners (`REGISTERED_ENTITY_TOKENS`: LLC/Inc/Corp/etc.) are tagged `is_entity` + `entity_name` instead of skipped — see `llc_lookup.py`. Trust names (`TRUST_TOKENS`) get a shot at local person-extraction first (`_try_extract_trust_person` — strips trust words/dates, applies the county convention) before falling back to a skip. Everything else non-individual (churches/banks/placeholders/ambiguous names) still gets an immediate `skip_reason`. Never guesses name order for an ambiguous single "Name" column — only the known county convention.
- `llc_lookup.py` — resolves an `is_entity` row to a real person via a live **Sunbiz** (Florida Division of Corporations) search in the same shared Chromium page as FOREWARN. `resolve_entity_owner()` returns an ordered `candidates` list rather than a single guess: the registered agent (if it parses as a plain person via the "LAST, FIRST" comma convention), OR — if the agent name is itself a company — the person embedded in a P.A./PLLC/PC-style entity name (`_try_extract_pa_person`, see build order #39) as the first candidate, with every officer/manager that parses as an individual kept as fallback candidates after it. `lookup_engine.process_row()` tries candidates in order, only spending a second real FOREWARN search on a fallback if the first comes back NOT_FOUND. Only trusts the "LAST, FIRST" comma convention Sunbiz uses for individual names, and never guesses word order on an ambiguous multi-person firm name — same caution `input_parser.py` uses. Confirmed working against the live site (see build order #18).
- `forewarn_lookup.py` — CLI entry point (same engine as the web app). Also calls `crm.init_db()` on startup now (needed for the weekly lookup counter table — see below), same as the web app already did.
- `crm.py` — SQLite-backed CRM: clients, notes, recurring follow-ups, **and manual calendar `events`** (separate table, merged into calendar/Dialer views rather than touching the recurring-schedule math). Also owns the **automatic backup system** (see below), the **weekly lookup counter** (`lookup_stats` table, keyed by that week's Monday — `get_weekly_lookup_count()`/`increment_weekly_lookup_count()`), **Transaction Manager** (see build order #22): `document_types` (shared per-type checklist + shared template file), `transactions`, `transaction_documents` (per-transaction snapshotted rows, independent of later checklist edits), and **Contact Lists** (see build order #27): `contact_lists` + `contact_list_members` — a named group of clients sharing one recurring call schedule (`next_call_date`/`interval_days` on the list itself, not per-member). Uploaded files live under `DOCUMENTS_DIR` (`documents/templates/`, `documents/signed/` — both gitignored) via `_store_upload`/`_delete_stored_file`. DB file `urto_crm.db` is gitignored — **never commit real client data**.
- `templates/index.html` — the entire frontend. One file, five tabs: **Home**, **Owner Lookup** ("URTO Skip Trace" in-app), **URTO CRM**, **URTO Dialer**, **Transactions**. All vanilla JS, no framework. Shared space-themed CSS (`.space-bg`, glass cards, gold glow) applies to every page.
- `static/urto-space.js` — Three.js WebGL scene for the Home tab, including the 3D skywriting rocket + particle smoke.
- `static/stars.svg` — small static starfield SVG used as the tiled background layer on non-Home pages.
- `static/vendor/three/` — **vendored Three.js + addons**, pulled from the npm tarball (`registry.npmjs.org`), not a CDN. `cdn.jsdelivr.net` and similar CDNs are **blocked by this environment's network policy**.

## Backup system (crm.py)

- **Multiple simultaneous destinations**: `resolve_backup_dirs()` returns EVERY place a backup gets written, not one-or-the-other — always the local `backups/` folder (gitignored, guaranteed baseline) PLUS OneDrive (auto-detected via the `OneDrive`/`OneDriveConsumer` env vars) PLUS Google Drive (see below) whenever each is present. `_write_backup()` loops over all of them; one destination failing (a cloud folder briefly locked) never blocks the others.
- **Google Drive**: detected via **Google Drive for Desktop's Mirror-mode local folder** (`_detect_google_drive_dir()` checks common default paths), NOT the real Google Drive API — deliberately chosen with Mario so this needs zero new account/OAuth setup on his end, same "just write into a locally-synced folder" trick OneDrive already uses. If auto-detect guesses wrong, Mario can paste the real path into a small form in the CRM tab's backup bar (`POST /api/backup/google_drive_dir`), stored in `app_settings` and preferred over auto-detection from then on.
- **Instant mirror**: every CRM mutation calls `on_data_changed()`, which immediately rewrites `urto_crm_latest.db`, the human-readable **CRM Contacts** export, and the **full JSON data export** (see below) to every destination — so deletions propagate right away, not just additions. All three are cheap (small file overwrites), so they run on every single edit, not just periodically.
- **Periodic snapshots**: `backup_now()` also writes a timestamped `urto_crm_YYYYMMDD_HHMMSS.db` snapshot (capped at 30, oldest pruned) to every destination — run on app startup, on the "Backup Now" button (`/api/backup/run`), and by a 60s background watcher.
- **CRM Contacts export** (`_write_crm_contacts_export`): one `.txt` file per client under `CRM Contacts/` in every backup destination — name/phone/address/follow-up status/full notes log, human-readable straight from OneDrive or Google Drive without opening URTO. Full diff each run (stale files for deleted clients are removed).
- **Full JSON data export** (`_write_full_data_export`): a single `URTO_Full_Data_Export.json` per destination containing EVERY table (clients, notes, contact lists + membership, calendar events, transactions + their documents, document checklists, lookup stats) — regenerated on every single instant mirror. Meant to be copy-pasted into a fresh Claude conversation as a complete description of every piece of data currently in URTO. Don't gate this behind the snapshot path — Mario explicitly wants it to change on every add/edit/delete, and it's cheap (a handful of SELECTs + `json.dumps`).
- **Transaction Manager's uploaded documents** are mirrored into two human-organized folders per destination (`_mirror_documents_folder`, real original filenames, not the internal stored/uuid ones): `Transaction Checklist Documents/<Type>/<Document Name>.<ext>` (the shared per-type templates) and `Transactions/<Title> (id)/<Document Name> - Template/Signed.<ext>` (one folder per actual transaction, across active/closings/archive). This stays on the periodic/snapshot path for ROUTINE mutations (real files are heavier than a DB overwrite) — **except** the specific document-mutating functions (`set_document_type_template`, `remove_document_type_template`, `delete_document_type`, `upload_signed_document`, `remove_signed_document`, `create_transaction`, `delete_transaction`) which each also call `_instant_document_mirror()` right after `on_data_changed()`, so an uploaded/removed document shows up in the cloud immediately rather than waiting up to 60s — without making every OTHER routine note/follow-up edit pay for real file I/O. Every mirror here is a full diff: copies new/changed files **and deletes ones no longer expected** (removed document, renamed/deleted transaction), so removals actually disappear from every backup destination too.
- Status surfaced via `/api/backup/status` (per-destination list, not a single bool) and a status bar in the CRM tab showing OneDrive/Google Drive/Local chips plus the Google Drive path-entry form when not auto-detected.
- **`CODE_DOWNLOAD_LINK.txt`** (`_write_code_download_link`): a plain-text pointer to the app's own GitHub ZIP/repo URL, written into every backup destination alongside the data (see build order #41) — so losing the PC entirely is a two-step recovery (GitHub for the code, any one backup folder for the data + this pointer), not something that depends on remembering a URL from somewhere else. Keep its `GITHUB_ZIP_URL` constant in sync with `urto_updater.pyw`'s `REPO_ZIP_URL` if the branch ever changes.

## Features (all working, verified)

1. **Owner Lookup / Skip Trace** — drag a CSV in (flexible column matching — see `input_parser.py` above), automates FOREWARN name+zip search, opens **every** same-name candidate's Address History page to verify the target address before taking a phone number (no shortcuts — see gotcha below), returns the top-ranked phone. Human-like typing/pacing. Stop button + partial-results download. **LLC/business-entity owners are resolved to a real person via Sunbiz first** (registered agent, or an officer/manager fallback), then searched on FOREWARN like anyone else — see `llc_lookup.py`.
2. **URTO CRM** — add/edit/delete clients (with an optional initial note on add), timestamped append-only notes log, automatic recurring follow-ups (2/3/4wk, 2/6mo, or custom interval) computed on-the-fly from a single anchor date per client. **Calendar tab**: click any day to add a manual event (title/date/time/notes, optionally linked to a client as a follow-up); click an existing manual event to edit/delete it; automatic recurring occurrences and manual events render together, visually distinguished. **Contact Lists tab**: group clients under a named list (e.g. one per building) with a single shared recurring call schedule for the whole group — separate from each client's own individual follow-up schedule; membership can be set from the list's "Manage Members" picker or from the client's own Add/Edit form.
3. **URTO Dialer** — power dialer: Enter calls the current entry via `tel:` link, arrows navigate. Sources: pasted text, CSV upload, CRM clients, last lookup results, **Today's Follow-ups** (auto-due-today + overdue clients, deduped with any manual event for today), or **From Contact List** (pick a saved Contact List — see CRM below — and dial through every member with a phone number, with a "✅ Mark this round as called" button to advance the list's shared schedule). Whenever the current entry matches a CRM client — by direct load or by phone-number cross-reference — an **"In your CRM"** panel shows their info, follow-up status, and notes live. Non-CRM entries get a **"+ Add to CRM"** button that opens the (prefilled) client form. A **"🎙 Dictate a call note"** button uses the browser's free built-in speech recognition to transcribe a spoken recap straight into that client's notes (typing fallback on browsers without speech support).
4. **Transaction Manager** — track a deal (Listing/Rental Listing/Buyer Representation/Rental) through closing. Each transaction shows a row per required document with a **Template** box (the type's shared blank form, view-only here — edited in "Manage Document Checklists") and a **Signed** box (upload/download/remove, per-transaction). Status pill: Active (green) / Under Contract (yellow) / Closed (gold). "Manage Document Checklists" (reachable from the transaction list) is where you add/remove documents from a type's checklist and upload/replace each one's shared blank template — those edits only affect transactions created afterward.
5. **Home** — full-viewport Three.js scene: starfield, nebulas, a chrome extruded 3D "URTO" wordmark, four orbs (Skip Trace/CRM/Dialer/Transactions) on tilted orbits that are the **only navigation** on this screen. Occasional rocket — now a real lit 3D model (nose/body/fins/nozzle/glass window), layered flame + spark particles, shaded volumetric smoke puffs — flies across trailing a skywriting exhaust plume reading "Made by Mario Bengochea".

## Hard-won gotchas (don't re-discover these)

- **FOREWARN's form fields use Material-UI floating `<label>`s, not native `placeholder` attributes.** Use `page.get_by_label(...)`, never `get_by_placeholder(...)`.
- **The SEARCH button contains a nested `<span>`**, so `get_by_text()` hits a strict-mode "2 elements" error. Use `get_by_role("button", name=<case-insensitive regex>)`.
- **Never trust the results-list card's address for a match.** Always open each candidate's actual **Address History** page and check it before taking a phone number — a prior shortcut here caused a real wrong-person bug.
- **CDNs are blocked**; use `registry.npmjs.org` tarballs for any new frontend dependency.
- **CSV parsing must match columns by meaning, not exact name.** Real-world exports vary ("Zip Code" vs "Postal Code" vs "Zip"). Never blind-guess name order for a single ambiguous "Name" column — only the known Miami-Dade county "LAST FIRST" convention on a recognized combined owner-name column. Guessing wrong here is exactly the wrong-person bug class above.
- **Not every data source uses the county's "LAST FIRST" convention — some (e.g. IMAPP) ship a combined owner-name column in plain "FIRST LAST" order instead.** `input_parser._detect_name_order()` decides which convention applies ONCE per file, from a signature of columns unique to that kind of export (never per-row, never by guessing at a single ambiguous name) — see build order #34 for how this was actually confirmed (cross-referencing owner names against that same file's recorded-mortgage-borrower field). If a new aggregator export shows up with yet another name-order convention, add a new signature + `name_order` value rather than trying to auto-detect word order from the name itself. **Trust-owned rows are always parsed as "last_first" regardless of the file's detected name_order** — trust ownership names look to carry over from the raw county record even in an otherwise-first-last file (confirmed the same way). Don't change that exemption without similarly strong cross-referenced evidence.
- **A generic "Address"/"City"/"State" column can silently BE a mailing address, not the property's own.** County tax-roll exports for an LLC/entity-owned parcel often carry both — a mailing address (where the owner/registered agent's tax bill goes) and a separate situs/site/property address for the actual parcel. `HEADER_ALIASES` in `input_parser.py` lists situs/site/property aliases FIRST (ahead of the generic ones) for exactly this reason, and `resolve_headers()` hard-excludes any column whose name contains "mailing" outright. Don't reorder those aliases back or relax the mailing exclusion without re-reading build order #30 — that's the exact shape of bug that already shipped once (right person, right phone, wrong property).
- **3D rocket meshes need `depthTest: false`** on their materials (with staggered `renderOrder`) — otherwise they get incorrectly occluded by the logo/orbs/rings that share the same depth range, since the rest of the Home scene uses real opaque 3D geometry.
- **`crm.py`'s backup globals need `global _dirty, _last_backup`** in `backup_now()` — a missed `global` on a reassigned module-level dict silently shadows it with a local, so the function *works* (returns the right value) but the module-level state never updates. Bit us once; if a status endpoint ever looks "stuck," suspect this pattern first.
- **Manual calendar events are a separate `events` table**, deliberately not folded into the recurring-followup math — keeps ad-hoc events from disturbing a client's automatic schedule anchor. `get_calendar_events()` merges both into one list tagged by `kind` ("auto"/"manual").
- **The Dialer's Enter/arrow keyboard shortcuts must not fire while a modal is open** ("Add to CRM" sits on top of the Dialer) — guarded via `document.querySelector('.modal-overlay.show')` in the keydown handler.
- WebGL testing in this sandbox: `p.chromium.launch(executable_path="/opt/pw-browsers/chromium", args=["--enable-unsafe-swiftshader","--use-gl=angle","--use-angle=swiftshader"])`. A hovered orb intentionally slows its own orbit (`orb.hoverT`) so it's easier to click. **The scene's game-clock (`elapsed`) is heavily throttled in a headless/backgrounded browser** (rAF fires far slower than wall-clock) — for anything timing-gated (like the rocket), temporarily force the trigger via a debug hook (e.g. `window.__flybyDebug().nextAt = 0`) rather than waiting real seconds, and strip the hook before committing.
- **`search.sunbiz.org` is blocked by this sandbox's network policy** (`ERR_TUNNEL_CONNECTION_FAILED`) — same restriction that already applies to FOREWARN, documented in README's "Known fragile spots". `llc_lookup.py`'s selectors/text-labels were written from Sunbiz's known, long-stable page structure, verified in this sandbox only against **local HTML fixtures** standing in for the real site — but confirmed working against the actual live site on Mario's first real run (see build order #18). If a future row still fails to resolve unexpectedly, use `--debug` (threads through from the CLI's existing `--debug` flag into `resolve_entity_owner`) to pause with Playwright Inspector at the search box and again after the results/detail page loads.
- **Sunbiz officer/registered-agent names only parse when the "LAST, FIRST" comma convention is present** (`_split_person_name` in `llc_lookup.py`) — deliberately refuses to guess word order otherwise, matching `input_parser.py`'s existing name-order caution. It also handles the 3-segment `"LAST, SUFFIX, FIRST"` shape (e.g. `"HACKETT, II, JOHN"`) as its own explicit case via `_NAME_SUFFIXES` — do NOT try to generalize that into "just take the last comma segment as first name," since a name with a real middle name after a 2-segment split (`"LAST, FIRST MIDDLE"`) would then break. If Mario reports a real LLC row skipping with "couldn't confidently resolve" and the Sunbiz page clearly shows a resolvable person, the name format on the live page is probably not one of these two recognized shapes — check with `--debug` before loosening anything.
- **A registered-agent-service company (CT Corporation System, Registered Agents Inc, etc.) must not be mistaken for a person** — `_looks_like_company()` checks both `ENTITY_TOKENS` and a small `AGENT_SERVICE_KEYWORDS` list before falling back to the officers/managers list. `ENTITY_TOKENS` includes professional-entity suffixes (`PLLC`/`PA`/`PLC`/`PC`) since law/professional firms commonly serve as someone's registered agent and would otherwise get misparsed as a person's name (e.g. `"NEIMAN & INTERIAN, PLLC"` → garbage `"Pllc Neiman & Interian"`). If a new agent-service firm or entity suffix shows up as a false "person" match, add its name fragment to `AGENT_SERVICE_KEYWORDS`/`ENTITY_TOKENS` rather than loosening the entity-token check itself. **`_looks_like_company()`'s tokenizer strips periods with no replacement (not a space)** before splitting — a dotted abbreviation like `"P.A."` or `"L.L.C."` must collapse to `"PA"`/`"LLC"` to match `ENTITY_TOKENS`; replacing periods with spaces instead (the old, buggy behavior) splits it into single letters that never match anything. If a new dotted suffix shows up unrecognized, check this collapsing step before assuming the token itself is missing from `ENTITY_TOKENS`.
- **A P.A./PLLC/PC-style registered agent is itself named after the actual licensed professional** (Florida requires this) — `_try_extract_pa_person()` strips a trailing `PA_STYLE_SUFFIXES` token and reads the rest as that person's own plain "First Last" name, and `resolve_entity_owner()` puts it FIRST in the returned `candidates` list (searched on FOREWARN before the slower officer/manager fallback, not instead of it — see build order #39, an explicit Mario design call: "keep BOTH options... higher success rate"). Refuses to extract when `"&"` is among the remaining tokens (a multi-partner firm name like `"NEIMAN & INTERIAN, PLLC"` doesn't belong to one single person) — don't remove that guard, it's the same word-order-ambiguity caution used everywhere else in this file.
- **Trust-name person extraction (`_try_extract_trust_person`) only strips a fixed `TRUST_STOPWORDS` set and a trailing date token** — a trust suffix phrasing not in that set will just fail to strip and fall through to the surname-only skip, not silently guess wrong (this already bit one real row — `"...REV TRUST AGR <date>"` stopped stripping at the then-unrecognized `"AGR"`, leaving `"TRUST"` stuck mid-list and tripping the entity-token rejection; fixed by adding `AGR`/`AGMT`/`REV`/`LIV`/`TR` to `TRUST_STOPWORDS`). If a real trust row should've resolved but didn't, check what trailing words are actually in the raw name and add them to `TRUST_STOPWORDS` rather than loosening the "must have 2+ tokens left" rule. Conversely, when a trust's name genuinely isn't in the county's "LAST FIRST MIDDLE" convention (some are self-authored in plain first-middle-last order), don't guess — flag it instead, the same as any other ambiguous name order in this file.
- **The weekly lookup counter increments once per actual FOREWARN search** (inside `lookup_engine._run_forewarn_search`, not per input CSV row) — a row `SKIPPED` at input-parsing time, or an `is_entity` row whose Sunbiz resolution fails, returns before ever reaching that function and correctly doesn't count. If the count ever looks off, check whether something new is short-circuiting before `_run_forewarn_search` (should not count) or after it (should count). A fallback candidate retry (see the P.A./PLLC gotcha above) is a genuinely separate FOREWARN search, so it correctly counts again.
- **`urto_crm.db` and `documents/` live in `%LOCALAPPDATA%\URTO`, NOT inside the project folder** (`crm.py`'s `DATA_DIR`/`_resolve_data_dir()`) — this was a real bug once (Mario's weekly lookup counter reset, see build order #38): every project-folder-relative path is at risk of vanishing the moment an update lands in a freshly-extracted folder instead of the one currently in use. `_migrate_legacy_data_dir()` runs once on import and copies forward an old in-project-folder DB/documents folder the first time this fix's code runs, so existing data isn't lost — but never move `DATA_DIR` back to being project-folder-relative, and if a future feature adds any new persisted file, put it under `DATA_DIR`, not next to `crm.py`.
- **Backups write to a LIST of destinations (`resolve_backup_dirs()`), never just one** (see build order #40) — always Local, plus OneDrive and/or Google Drive whenever detected/configured, all three able to be present simultaneously. If you add any new kind of backup export, loop over `resolve_backup_dirs()` like `_write_backup()` already does — don't special-case "the" backup dir singular, that assumption is gone. Google Drive detection is a **local folder path guess** (Google Drive for Desktop's Mirror mode), not the real Google Drive API — there's no env var Google sets the way OneDrive does, so a wrong guess is expected sometimes; that's why the manual override (`set_google_drive_dir`, stored in `app_settings`) exists and always wins over auto-detection.
- **The full JSON data export (`URTO_Full_Data_Export.json`) and the CRM Contacts `.txt` files are deliberately on the INSTANT mirror path, not gated behind the snapshot cadence** — they're cheap (small text, a handful of SELECTs), and Mario explicitly wants them to reflect every single add/edit/delete, not just whatever the periodic snapshot last caught. Don't move them to the snapshot-only path to "optimize" — that would silently reintroduce the staleness he specifically asked to avoid.
- **Transaction Manager's document checklist is only ever seeded once**, gated by an `app_settings` row (`document_types_seeded`), not by "is the table empty" — Mario deliberately deleting every document off a type down to zero must never cause them to silently reappear on the next restart. If you ever add more seed data, extend `DEFAULT_DOCUMENT_TYPES` and don't touch the seeding gate.
- **A transaction's documents are a snapshot, not a live join** — `transaction_documents.name` is copied at creation time and `document_type_id` is `ON DELETE SET NULL` (not `CASCADE`). Deleting or renaming a document from a type's checklist later must never change or orphan an already-created transaction's row or its uploaded signed file. Only the shared **template** (via the still-linked `document_type_id`) can disappear from an existing transaction's view if the type-level template is later removed — the transaction's own signed upload is never affected either way.
- **Document-folder backup mirroring intentionally only runs on the snapshot path** (`_write_backup(..., snapshot=True)`), not on every `on_data_changed()` instant-mirror call — see "Backup system" above. If a new document-related mutation needs to show up in backups faster than ~60s, don't naively add a full folder copy to the instant path; that's real file I/O on every single edit, unlike the tiny SQLite overwrite the instant path was designed around.
- **`urto_updater.pyw` kills the old server by matching the process's command line** (`Get-CimInstance Win32_Process | Where-Object CommandLine -like '*app.py*'`), never by process **name** (`pythonw.exe`/`python.exe`) — Mario could have other unrelated Python programs running, and killing by name would take those down too. If a future change ever needs to find/stop the running URTO process elsewhere, reuse this same command-line-match approach, not a name-based one.
- **Never hand-type `APP_VERSION_DATE`/a "last updated" timestamp again** (see build order #43) — it always drifts from Mario's real local time since I can't know his actual clock at ship time. The footer's date comes from `app.py`'s `_get_last_updated_display()`, which reads `last_updated.txt` (written by `urto_updater.pyw` using the installing PC's own clock) — if a future feature needs a "when did this last happen" timestamp anywhere in the UI, follow this same pattern (read it from the machine it's actually running on) rather than typing a guess into source.
- **`app.py`'s auto-close (`_watchdog`/`/api/shutdown`) always checks `_job_in_progress()` before exiting** (see build order #42) — URTO, unlike Wardrobe, can have a real headed Chromium window mid-FOREWARN-scrape in a background thread when the tab closes or the heartbeat lapses. Never remove that guard or shorten `HEARTBEAT_TIMEOUT` in a way that could fire mid-lookup; a killed process there abandons the scrape with no partial-results file ever written, exactly the kind of silent data loss this whole file warns against elsewhere.

## Don't do this

- Don't introduce React/Tailwind/shadcn/TypeScript/a build step — evaluated once already and explicitly rejected in favor of staying single-file-vanilla so Mario's one-click update flow (the "URTO Updater" desktop icon) keeps working.
- Don't commit `urto_crm.db`, `outputs/`, `flasklog.txt`, or `backups/` — all gitignored, keep it that way.
- Don't add telephony/Twilio integration unless explicitly asked — the Dialer is deliberately just `tel:` click-to-call (free, no account, no TCPA/compliance surface).
- Don't reintroduce paid AI call-summarization (Anthropic API calls per dictation) — explicitly tried and declined by Mario over cost/account concerns. The free browser-speech dictation feature is what shipped instead. If he asks for AI summarization again, get explicit confirmation he understands it needs a separate paid Anthropic API account before building anything.
- Don't build the "cloud-hosted CRM" idea (discussed with Mario: split CRM+Calendar onto a small hosted server with a real database, separate from the local Skip Trace/Dialer) without him first creating a hosting account (e.g. Render) — that step can't be done on his behalf.
